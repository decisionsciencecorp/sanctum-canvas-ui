# A7.4–A7.6 evidence — PHP inference, tools, persistence

**Date:** 2026-09-18  
**Tasks:** #4164, #4165, #4166  
**Repo:** `sanctum-canvas-ui`  
**Lab page:** not in this slice (A7.7)

## Delivered

### A7.4 Inference stream (`public/api/chat.php`, `src/Php/Inference/`)

- Authenticated JSON: `messages`, `runId`, `libraryId` / `libraryVersion`, optional `existingProgram`, `format` (`ndjson`|`sse`), `provider`, `fixtureId`
- Server-side library prompt via `LibraryPromptInjector` + `PromptCompiler` (client `system` messages discarded)
- Provider-neutral `InferenceProvider` + `StreamEvent` AG-UI vocabulary
- Adapters: `FakeProvider`, `FixtureProvider`, `VeniceProvider`
- Venice key from env only: `VENICE_API_KEY` / `VENICE_INFERENCE_KEY` (document pass file `~/.ssh/venice-api-moya.pass` — never hardcoded)
- Caps: duration + bytes; disconnect stop; failure redaction

### A7.5 Tool registry (`public/api/tools.php`, `src/Php/Tools/`)

- Fixed allowlist (`ToolRegistry::labDefaults`: `echo_read`, `note_get`, `note_set`, `note_delete`)
- Auth + project scope; read/write/destructive; JSON Schema args; CSRF on writes; rate limit; idempotency; confirmation for destructive; redacted audit
- Unknown / hostile names rejected before handlers (callables, URLs, SQL/shell shapes)

### A7.6 Persistence (`public/api/programs/*`, `src/Php/Storage/`)

- `SqliteProgramStore` (idempotent migrations) and `FileProgramStore`
- save / get / patch / rollback / revisions
- Owner/project from auth only (IDOR); CSRF on mutating ops; size limits

## Tests

```bash
./vendor/bin/phpunit
# Lines (src/Php): ≥90% (measured ~93% with PCOV on 2026-09-18)
```

Security suite: `tests/php/Security/ToolSecurityTest.php`.

## Smoke (local)

```bash
# Auth header: X-Canvas-Auth: canvas-lab-dev (default lab token)
# CSRF: GET /api/csrf.php then X-CSRF-Token on writes
# Chat fixture: POST /api/chat.php {"runId":"r1","fixtureId":"text-message-basic","messages":[{"role":"user","content":"hi"}]}
# Tools: POST /api/tools.php {"tool":"echo_read","arguments":{"message":"hi"}}
```
