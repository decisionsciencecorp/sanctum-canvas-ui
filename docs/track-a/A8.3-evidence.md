# A8.3 evidence — security / fuzz / abuse audit

**Date:** 2026-09-18  
**Task:** [#4171](https://tasks.decisionsciencecorp.com/admin/view.php?id=4171)  
**Doc #1379 §9:** automated regression in `tests/security/` + PHP twins

## Commands

```bash
npm run test:security
# 74 pass / 0 fail

vendor/bin/phpunit tests/php/Security tests/php/Http
# 46 pass (PHP twins: ToolSecurity, CSRF, RateLimiter, ErrorRedactor)
```

## Coverage vs Doc #1379 §9

| Topic | Suite / twin | Result |
|-------|--------------|--------|
| Parser bombs / depth / cyclic / SSE buffer | `parser-bomb.test.js` | pass |
| XSS / markdown allowlist / unknown component | `xss.test.js` | pass |
| Encoded unsafe URLs / CSS url / hosts / OpenUrl | `url.test.js` | pass |
| CSP header + meta (no unsafe-inline/eval) | `csp.test.js` | pass |
| Partial tool / gesture / TOOL_CALL_ARGS / hostile names | `tool-abuse.test.js` + PHP ToolSecurity | pass |
| CSRF / IDOR scope | `csrf-idor.stub.test.js` + HttpKernel / Persistence | pass |
| Rate limits | `rate-limits.stub.test.js` + RateLimiter twin | pass |
| Secret / error leakage | `secret-leakage.stub.test.js` + ErrorRedactor | pass |
| Track B handoff fixtures | `handoff-fixtures.test.js` | pass |

## High-risk findings

**None open.** Hostile fixtures under `tests/fixtures/hostile/` stay inert (no executable DOM, no tool invoke from partial/incomplete programs). Prior “named-host e2e soak” items are **follow-on hardening**, not open high-risk defects — local §9 invariants are regression-gated.

## Result

| Gate | Result |
|------|--------|
| `npm run test:security` green | **Yes** — 74/74 |
| PHP security twins green | **Yes** — 46/46 |
| Open high-risk findings | **None** |

**Status:** **GREEN** — A8.3 closed.
