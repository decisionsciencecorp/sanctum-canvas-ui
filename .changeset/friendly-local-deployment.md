---
"@openuidev/devtools": patch
---

Offer a dismissible deployment command and guide after a valid local renderer response. The hint is development/loopback-only, shown once per browser origin, and adds no network analytics. Disable it with `deployHint={false}`.
